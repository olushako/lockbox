version: '3.8'

services:
  admin-container:
    image: docker:cli
    container_name: admin-container
    command: sleep 3600
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
